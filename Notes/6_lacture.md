whenever the state variable update react trigger the recosiliation cycle and it rerender the componenet
---
use effect

if we don't pass dependency array in use effect the eseeffect is called on every render

if we have empty array then it will call only on initial render


If we have any value then it will render during it


single page applocation


two types of routing

1. client side routing : when we are not hitting server to get the data for web page.

2. server side routing: when we need to call server.




